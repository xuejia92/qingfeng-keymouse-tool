# 循环与控制流模块 — 完整设计方案

> 覆盖三个「块」类型：**if 条件分支**（已有）、**foreach 循环**、**while 循环**。
> 三者共用同一套「扁平列表 + 类型配对」的存储、执行与交互机制，风格完全一致。
> 本文在上一版 foreach 方案基础上，新增 while，并把「块」抽象为通用模型。

---

## 0. 总览：统一块模型

现有 `if/elseif/else/endif` 已经是一套块机制。新增 `foreach`、`while` 后，一共三种
起始块，全部用**类型 + 栈匹配**配对，序列化仍是扁平 `steps` 列表，老文件零迁移。

| 块 | 起始 | 结束（自动生成） | 分支头 | 面板可见 | 可配置参数 |
|---|---|---|---|---|---|
| 条件分支 | `if` | `endif` | `elseif` / `else` | ✅ | `condition` |
| foreach 循环 | `foreach` | `endForeach` | 无 | ✅ | `items` / `item_var` / `index_var` |
| while 循环 | `while` | `endWhile` | 无 | ✅ | `condition` |

```python
# 类型配对表（conditions.py）
BLOCK_PAIRS = {"if": "endif", "foreach": "endForeach", "while": "endWhile"}
BRANCH_HEADS = {"if": ("elseif", "else")}          # 只有 if 有分支头
```

```python
# 自动成对生成、不在面板展示的结束标记（config.py）
AUTO_STEP_TYPES = {"endif", "endForeach", "endWhile"}

# 显示名（config.py FLOW_STEP_TYPES 追加）
"foreach": "循环",      "endForeach": "循环结束",
"while": "当循环",      "endWhile": "当循环结束"
```

---

## 1. foreach 循环（要点，详见上一版）

- 参数：`items`（数据源变量，只读下拉）、`item_var`（默认 "item"）、`index_var`（默认 "index"）。
- 执行：对 `items` 的每个元素写 `item_var`/`index_var` 后执行循环体，空列表直接跳过。
- 数据源在块开头取一次列表引用快照。

---

## 2. while 循环（新增）

### 2.1 结构与参数

```python
# default_step_params 新增
if step_type == "while":
    return {"condition": ""}      # 条件表达式，语法与 if 完全一致
if step_type == "endWhile":
    return {}
```

- `while` 起始块，面板可见，配置一条**条件表达式**（语法、翻译、求值、变量校验全部
  复用 `if` 的 `translate_condition` / `eval_condition` / `check_condition_variables`）。
- `endWhile` 结束块，随 `while` 自动成对，不在面板。

### 2.2 执行语义（与 foreach 的差异）

- 进入 `while`：求值 `condition`；**不成立** → 直接跳到 `endWhile + 1`（一次都不执行）；
  **成立** → 进入循环体。
- 到达 `endWhile`：无条件**跳回配对的 `while`**，重新求值条件，直到不成立或超限/手动停止。
- 循环体内不自动注入任何变量（没有 item/index 的概念）；要退出循环，用户应在体内用
  条件判断 + 修改变量，让 `condition` 最终变为假。

### 2.3 死循环保护（while 特有，关键安全点）

foreach 天然有界（列表长度），while 无界，必须有保护：

```python
MAX_WHILE_ITERATIONS = 10000      # conditions.py 全局常量（待确认，见 §8）
```

执行引擎里每个 while 帧记录 `iterations`，每回到 `endWhile` 自增；超过上限即报错：

```python
if frame["iterations"] >= MAX_WHILE_ITERATIONS:
    return f"第 {idx+1} 步「当循环」超过 {MAX_WHILE_ITERATIONS} 次迭代，疑似死循环，已终止"
```

`_stop` 手动停止检查沿用现有逻辑，每次迭代开头检查，及时退出。

### 2.4 表单（flow_dialog.py）

`while` 分支与 `if` 几乎一致：条件表达式输入框 + 语法提示 + 引导文案
（「条件成立则反复执行 while 与 endWhile 之间的步骤，直到不成立」）；`accept()` 校验
条件必填、引用变量已定义（复用 `check_condition_variables`）。`endWhile` 分支同
`endif`，只放提示。

---

## 3. 通用能力（三块共用，type 驱动，无需逐块重写）

以下函数对 if / foreach / while 一视同仁，靠 `BLOCK_PAIRS` 驱动：

### 3.1 配对与逻辑树（conditions.py）

```python
def match_block_end(steps, open_idx) -> int | None   # 栈匹配，支持交叉嵌套
def enclosing_block(steps, idx) -> int | None        # 最内层起始块
def block_indices(steps, idx) -> list[int]           # 整块区间
def build_block_tree(steps) -> list                  # 逻辑树（内存推导，不落盘）
```

### 3.2 缩进（通用化 condition_indent_levels → block_indent_levels）

规则不变：`if/foreach/while` 及其结束标记作为骨架**不缩进**，体内步骤缩进一级；
`elseif/else` 分支头与配对的 if 对齐；嵌套自然叠加。左侧 1px 浅蓝连接线（可选）。

```text
1. 🔀 条件判断            ← 0
2. 🔄 循环 names          ← 0
3.     ♻️ 当循环 i<3      ← 1（foreach 体内嵌套的 while）
4.         📄 日志输出 i  ← 2（while 体内）
5.     🏁 当循环结束      ← 1（与内层 while 对齐）
6. 🏁 循环结束            ← 0
```

### 3.3 交互（flow_tab.py）

- **拖入成对**：拖 `foreach`/`while` → 自动插入起始块 + 结束块两个 FlowStep（照抄 if）。
- **边界回滚**：`_on_order_changed` 读回新顺序后 `validate_block_structure` 不通过 → 回滚
  + 状态栏提示「不能把步骤拖出循环/条件块边界」，保证闭合完整。
- **删除整块**：删 `if`/`foreach`/`while`（或对应结束标记）→ `block_indices` 整段剔除，
  内部子模块与结束标记一并删除；确认弹窗文案按块类型说明「同时删除整个 X 块的 N 个步骤」。
- **单步执行**：`endForeach`/`endWhile`/`endif` 不执行；`foreach` 取首项提示；
  `while` 只求值一次条件并提示真假，不真正循环。

### 3.4 校验（validate_block_structure）

| # | 规则 | 级别 |
|---|---|---|
| 1 | 起始块必须有配对结束标记（if→endif / foreach→endForeach / while→endWhile） | 错误 |
| 2 | 结束标记必须配对上方的起始块（孤儿） | 错误 |
| 3 | 交叉错位（不同类型块交叉闭合） | 错误 |
| 4 | 数据源/条件引用的变量未定义 | 错误（表单即时拦） |
| 5 | 空循环体（foreach/while 与结束标记之间无步骤） | 警告 |
| 6 | 嵌套深度超过 `MAX_BLOCK_DEPTH=8` | 错误 |

### 3.5 执行引擎（flows.py）

`_run_once` 增加 `loop_stack`（foreach 帧 / while 帧各自维护）：

```python
# foreach 帧：{items, i, body_start, end}
# while  帧：{body_start, end, iterations}
```

- 空列表 / 条件不成立 → 跳过整块；
- `endForeach`：`i+1` 未越界回 `body_start`，越界弹栈；
- `endWhile`：`iterations+1`，超限报错，否则回 `while` 重新求值；
- foreach/while 与 if 交叉嵌套时，`loop_stack` 与现有 `pending_ends` 相互独立、正交。

变量作用域：item/index 是普通运行时变量（foreach），循环结束保留最后值；while 不注入
变量。外层 `Flow.loops`（整体轮数）与 foreach/while 正交。

---

## 4. 序列化格式（不变）

`foreach` / `while` 只是 `steps` 数组里多一种 type，`asdict(flow)` 平铺即可：

```json
{
  "steps": [
    {"type": "var",     "params": {"name": "i", "type": "integer", "default_value": "0"}},
    {"type": "while",   "params": {"condition": "i < 3"}},
    {"type": "log",     "params": {"variables": "i"}},
    {"type": "var",     "params": {"name": "i", "default_value": "i + 1"}},
    {"type": "endWhile"}
  ]
}
```

`flow_from_dict` 只需确保新 type 已在 `FLOW_STEP_TYPES` 登记（否则 `__post_init__` 抛
未知类型而跳过该步）。加载后可加 `repair_blocks`：补齐缺失结束标记、丢弃孤儿结束标记。

---

## 5. 边界情况

| 场景 | 处理 |
|---|---|
| 空循环体（foreach/while） | 校验警告；运行期跳过/不空转 |
| foreach 数据源未定义 / 非 list | 表单拦截；运行期报「数据源不是列表」 |
| foreach 体内修改 items | 开头快照，本轮次数不变 |
| while 条件变量未定义 | 表单拦截（复用 check_condition_variables）；运行期 eval 报「变量未定义」 |
| while 死循环 | `MAX_WHILE_ITERATIONS` 超限报错终止 |
| 手动停止 | 每次迭代开头检查 `_stop`，及时退出 |
| 深层嵌套 | `MAX_BLOCK_DEPTH=8` 报错；缩进逐层叠加 |
| 手动改 json 产生孤儿/交叉 | `repair_blocks` 加载修复 + `validate_block_structure` 兜底 |

---

## 6. 改动文件清单与测试计划

| 文件 | 改动 |
|---|---|
| `app/config.py` | `FLOW_STEP_TYPES` + `AUTO_STEP_TYPES` + `default_step_params`（foreach/endForeach/while/endWhile）+ `summary()` 各 case |
| `app/conditions.py` | `BLOCK_PAIRS`/`BRANCH_HEADS`/`match_block_end`/`enclosing_block`/`block_indices`/`build_block_tree`/`block_indent_levels`/`build_loop_flow`/`validate_block_structure`/`max_block_depth`/`MAX_WHILE_ITERATIONS` |
| `app/flows.py` | `_run_once` 加 `loop_stack` 处理 foreach/while 与其结束标记 |
| `app/ui/flow_tab.py` | `MODULE_GROUPS` 加 foreach/while；拖入成对；边界回滚；整块删除；缩进用 `block_indent_levels` |
| `app/ui/flow_dialog.py` | `_TYPE_ICONS` + `_build`/`_fill`/`apply_to`/`accept` 加 foreach/while 分支、endForeach/endWhile 提示 |
| `tests/test_conditions.py` / `tests/test_flow_tab.py` | 配对/树/缩进/校验/交互用例 |
| `tests/test_foreach_steps.py`、`tests/test_while_steps.py`（新） | 端到端执行：单层/嵌套、空体、非 list、死循环保护、手动停止、体内套 if |

---

## 7. 图标建议

| type | 图标 | 说明 |
|---|---|---|
| `foreach` | 🔄 | 遍历循环 |
| `while` | ♻️ | 条件循环（避免与 elseif 的 🔁 冲突） |
| `endForeach` / `endWhile` | 🏁 | 与 endif 同款结束标记 |

---

## 8. 待你拍板的默认值

1. 循环项/索引变量名默认 `item`/`index`，自由输入（还是限定只能选已有变量？）
2. 循环结束 item/index 变量**保留最后一项的值**（还是清空？）
3. 空循环体按「警告 + 跳过」，不空转
4. 最大嵌套深度 **8 层**
5. **while 死循环上限 `MAX_WHILE_ITERATIONS = 10000`**（是否做成 while 表单里可配置？）
6. 折叠/展开：第一阶段只做「缩进 + 连接线」，折叠放第二阶段
7. 图标：foreach `🔄`、while `♻️`、结束标记 `🏁`

确认后我按 §6 清单逐文件实现，沿用测试隔离约定，不碰 `flows/` 与真实 `config.json`。
