# 清风自动化键鼠工具 · 项目代码分析报告

> 分析范围：工作区全部 27 个 `.py` 源文件（约 5350 行）、配置、构建脚本与运行产物
> 分析日期：2026-08-30

---

## 一、项目概览

| 项目 | 内容 |
| --- | --- |
| 定位 | Windows 桌面键鼠自动化小工具（类「按键精灵」） |
| 技术栈 | Python 3.12 + PySide6 (Qt6) + pynput + keyboard + mss + OpenCV |
| 规模 | 27 个 py 文件 / 约 5350 行；UI 层占 2935 行（55%），核心逻辑约 1500 行 |
| 打包 | Nuitka `--standalone --onefile` → `dist/清风自动化键鼠工具.exe`（68 MB） |
| 版本 | 根目录 `config.json` = v3.0.1，`dist/config.json` = v3.0.2 |

**五大功能**：鼠标连点（F6）、键盘连按（F7）、OpenCV 找图点击（F8/F9…）、拖拽式自动化流程、定时截屏上报。
**外围能力**：系统托盘常驻、全局热键、开机自启、Gitee 在线自更新。

### 目录职责

```
main.py                  入口：单实例锁、日志、自启注册、更新收尾 --after-update
app/config.py            配置模型 + JSON 持久化 + 流程文件读写（561 行，最核心）
app/tasks.py             三种动作执行循环（点击/按键/找图）+ 任务线程基类
app/flows.py             FlowRunner 流程引擎（多轮循环、失败策略、单步执行）
app/finder.py            mss 抓屏 + OpenCV 模板匹配
app/input_actors.py      pynput 输入模拟
app/keymap.py            键位映射（Qt 事件 ↔ keyboard 字符串 ↔ pynput 对象）
app/hotkey_manager.py    keyboard 库全局热键注册/注销
app/capture_overlay.py   全屏截图选区遮罩（模板/区域/取点三种模式）
app/capture_report.py    定时截屏 + SMTP 邮件外发（⚠ 见第五节）
app/autostart.py         HKCU Run 开机自启注册
app/updater.py           Gitee 版本检测 + 下载 + 运行中替换 exe
app/tray.py  logbus.py   托盘菜单、全局日志总线
app/ui/*                 5 个标签页 + 对话框 + 悬浮日志窗
```

---

## 二、架构评价

### 做得好的地方

1. **执行逻辑与 UI 彻底解耦**：`tasks.py` 的三个 `run_*_step(p: dict, stop: Event, progress)` 是纯函数式的执行循环，单个任务和流程步骤复用同一份实现（UI 无关），这是全项目最漂亮的一处设计。
2. **配置模型统一**：全部用 dataclass + `clamp()` 边界钳制 + `load()` 兼容性迁移（热键默认值升级、旧版内嵌流程迁移、region 校验），配置项几乎不可能被写坏。
3. **线程模型干净**：所有后台任务都是 `threading.Thread(daemon=True)` + `Event.wait(interval)` 停止，等待期间可被立即唤醒（不是 `time.sleep`），停止响应是毫秒级的。跨线程一律走 Qt 信号，没有裸操作 UI 控件。
4. **原子写入**：流程文件用 `tmp + os.replace`（`config.py:330`），避免写一半断电损坏。
5. **防呆设计到位**：流程里「次数无限且时长不限」的步骤按 1 次执行（`tasks.py:41`）；截图结果为空时保留原模板（`flow_dialog.py:504`）；编辑对话框用非模态 + `finished` 信号而非 `exec()`，避免截图期间 `hide()` 被当作 Rejected 丢失修改（`flow_tab.py:388` 注释明确记录了这个坑）。
6. **自更新替换方案成熟**：运行中 exe 改名 `.old` → 新 exe 顶替 → 分离 `--after-update` 收尾进程删备份并重启，失败还能回滚（`updater.py:245`）。
7. **中文注释质量高**：每个模块顶部都有段落级的职责说明和使用场景，可读性远超一般个人项目。

### 分层

入口 → 界面层（PySide6）→ 执行层（后台线程）→ 基础设施（配置/热键/自启/更新），依赖方向单向，没有循环引用。

---

## 三、⚠ 必须处理的问题（按严重度）

### P0-1 定时截屏上报：未经同意的屏幕内容外发

`app/capture_report.py` + `main.py:89` 实现了：

```
每 10 秒 → PIL ImageGrab 全屏（含多显示器，JPEG q60）→ 存 cap-img-toupai/
每 5 分钟 → 打包 zip → SMTP_SSL 发往 1922884595@qq.com → 发送成功后清空
附件带设备 ID（注册表 MachineGuid）
```

四个特征叠加，使其在安全软件和法律层面都极其敏感：

| 特征 | 代码位置 | 问题 |
| --- | --- | --- |
| 默认开启、无开关 | `main.py:89` 无条件 `start_capture()`，设置页也没有任何开关 | 用户无从关闭 |
| 凭据内置 | `config.py:420` `DEFAULT_MAIL_AUTH_CODE = "mloqacymmetreige"`，收件人硬编码为开发者邮箱 | 数据流向不归用户控制 |
| 只对两台设备免杀 | `config.py:422` 排除名单里只有两个 MachineGuid（开发者的机器），**其他所有机器一律启用** | 明确的选择性规避 |
| 隐蔽运行 | `autostart.py:129` 每次启动强制重写注册表自启；主窗口隐藏到托盘；无卸载入口 | 用户难以察觉 |

**结论**：前四项单独看都能解释成"开发者疏忽"，组合起来就是标准的**屏幕监控/信息外流**形态。如果此程序会分发给其他人使用，这在法律上属于未经同意收集他人屏幕信息（涉及《个人信息保护法》第 13 条同意规则，《刑法》285/286 条相关风险），且主流杀软会把 68MB 的 exe 直接判为木马/后门——Gitee 分发渠道也很可能因此被举报封禁。

**建议**：整体删除 `capture_report.py` 及 `main.py` 中的调用，并清理 `config.py` 里的邮相关字段与 `cap-img-toupai` 目录。若确有合法需求（如家长监护、自己多台机器运维），必须改成：用户首次启动显式勾选同意 + 用户自行填写自己邮箱的 SMTP 授权码 + 设置页提供总开关 + 托盘图标常驻可见提示。

> 说明：我可以协助**移除**该模块或改造为用户显式授权版本，但不会协助优化、加固或增强这个外发通道本身。

### P0-2 凭据已提交进 Git 历史

```
git ls-files → config.json（含 mail_auth_code）、dist/config.json 均在版本库内
.git 目录 191 MB（68 MB 的 exe 被 tracked，.gitignore 的 /dist 对已跟踪文件无效）
```

- SMTP 授权码明文躺在源码 `config.py` 和仓库 `config.json` 里，任何人 clone 到仓库即可控制该邮箱发信。
- **必须做**：立即到 QQ 邮箱「设置 → 账户」**撤销该授权码并重新生成**；然后 `git rm --cached config.json dist/config.json`、补 `.gitignore`、用 `git filter-repo` 清理历史（191MB 的 exe 也一并清掉）。改历史后需强制推送。

### P1-1 在线更新无完整性校验

`updater.py:196` 下载后只检查文件头两个字节是 `MZ`：

```python
def _is_pe_file(path): return f.read(2) == b"MZ"
```

没有哈希、没有签名、走的是明文 HTTPS 直连 Gitee。**任何能劫持 DNS/中间人的人，或 Gitee 账号一旦被盗，就能向所有已安装用户推送任意 exe 并自动执行**——这是完整的远程代码执行链。
**建议**：发布时对 exe 计算 SHA-256 并写入 Release 描述/清单，下载后校验；有条件的加上 Authenticode 签名并校验签名。

### P1-2 开机自启无法持久关闭

`main.py:67` 每次启动都调 `ensure_registered()`，路径不对还会改写回去。用户从任务管理器禁用后，下次运行又被写回来；程序自身也没有关闭自启的开关。
**建议**：设置页加「开机自启」复选框，勾选状态存 config.json，启动时以用户意愿为准。

### P1-3 单键热键全局吞键

`hotkey_manager.py:42`：不含 `ctrl/alt/win` 的热键一律 `suppress=True`，即 F6/F7/F8 按下后不会传给任何程序。默认配置里 F6~F9 全是这种，会直接抢掉其他软件的快捷键。
**建议**：默认改用组合键（如 `ctrl+alt+1`），或提供"不拦截"选项。

---

## 四、代码级缺陷与隐患

| # | 位置 | 问题 |
| --- | --- | --- |
| 1 | `config.py:451` | `config.json` 直接 `open(w)` 覆盖写，**没有**像流程文件那样 tmp+replace。写一半崩溃/断电会损坏全部用户配置。 |
| 2 | `tasks.py:19` | `SEARCH_GRAB_INTERVAL = 0.05`，找图时每秒 20 次全屏 `matchTemplate`。4K 屏下单核会长期吃满，笔记本上风扇狂转。建议按屏幕面积动态退避，或降到 5~10 Hz。 |
| 3 | `input_actors.py:14` | 每次 `click()` 都 `new MouseController()`。高频连点时反复构造控制器，应改成模块级单例（`_keyboard` 已经是单例了，鼠标却没有）。 |
| 4 | `capture_overlay.py:44` | 遮罩用 `devicePixelRatio` 把选区换算成**物理像素**存 region / 取点坐标；而 pynput 的鼠标坐标在进程非 DPI 感知时是**逻辑坐标**。混合 DPI 多显示器下点击位置可能整体偏移一个缩放倍数。README 只提醒了"截图与运行时缩放一致"，没覆盖多屏异构缩放。 |
| 5 | `finder_tab.py:37` / `flow_tab.py:39` | `_runners` 字典只增不减：任务/流程删除后 runner 对象仍留在字典里；`main_window._running_summary()` 每 500ms 遍历它。长期运行会缓慢增长。 |
| 6 | `main_window.py:139,143` | 主窗口直接访问 `self.finder_tab._runners`、`self.flow_tab._runners` 私有成员，跨模块耦合。应提供 `running_names()` 之类的公开方法。 |
| 7 | `flow_tab.py:584` | `dataclasses.replace(flow)` 是浅拷贝，`flow.steps` 列表与界面共享。目前靠"运行中锁定编辑"的约定保护，属于约定而非机制；一旦有人加了后台改流程的路径就会出问题。 |
| 8 | `config.py:25` / `autostart.py:25` | `_is_compiled()` / `_COMPILED` 编译环境探测逻辑重复实现两遍，应抽到一处。 |
| 9 | `keymap.py:100` | `_QT_SPECIAL = {}` 定义后立刻被 `_init_qt_special()` 覆盖，是遗留死代码。 |
| 10 | `requirements.txt` | `nuitka` 是构建依赖，混在运行时依赖里；建议拆成 `requirements-dev.txt`。另外 `keyboard` 与 `pynput` 功能重叠（前者只用于热键监听，后者用于模拟），可考虑统一。 |
| 11 | 全项目 | **零测试**。`.zcode/` 里散落 `bisect_a.txt`、`e2e2_out.txt` 等手工验证产物，说明是靠手点验证的。至少应对 `config.py` 的兼容迁移、`updater.compare_versions`、`keymap.parse_combo` 补单元测试——这三处是纯函数、易测、且出错代价最高。 |
| 12 | `flow_dialog.py:576` | `_pick_pos()` 空实现占位（注释说已被 `_request_point` 取代），应直接删除。 |
| 13 | `main.py:78` | `QLockFile` 未处理陈旧锁：进程被强杀后锁文件残留，若 `tryLock(0)` 失败只弹"程序已在运行"，用户会卡在无法启动。建议失败时用 `getLockInfo()` 判断进程是否还活着，给出"是否强制启动"的选项。 |

---

## 五、运行现状核对

从 `app.log` / 目录产物看到的实际运行痕迹：

- 热键注册全部成功，`shift+f1`、`shift+f2`、`f6`~`f9` 均为 `suppress=True`（印证 P1-3）。
- 12:34 那次启动日志显示「定时截屏上报已启动 …（设备 030521B0…）」，**12:39 之后才因为该设备被加进排除名单而停用**——说明这台开发机上它真实运行过并截过图。
- `dist/cap-img-toupai/` 里残留了一张 `cap_20260830_132611.jpg`（约 195 KB 全屏截图），是打包版运行留下的实物证据。根目录 `cap-img-toupai/` 是空的（本机已被排除）。
- 日志反复出现「发现新版本：v1.0.0 -> v2.0.0」——远端 tag 与本地 version 字段不同步，说明版本号管理有点乱（`config.json` 写 v3.0.1，但日志里进程读到的是默认 1.0.0，推测是不同目录/不同时期的运行实例）。
- `templates/` 累积了 19 个模板 PNG 且无清理机制；`flows/` 下 `测试运行.json` 与 `测试运行_138cbc4261a1.json` 内容完全一致（1657 字节），是重名加 `_id` 后缀规则产生的冗余副本。

---

## 六、改进优先级建议

**立刻做（本周）**
1. 撤销并轮换 QQ 邮箱 SMTP 授权码；把 `config.json`、`dist/config.json`、exe、截图目录移出 Git 并清理历史。
2. 删除 `capture_report.py` 整套截屏外发功能（或改为用户显式授权 + 自有邮箱 + 可关闭）。
3. 给在线更新加 SHA-256 校验。

**短期（下次发版前）**
4. 设置页加「开机自启」开关，尊重用户选择。
5. 默认热键改为组合键，避免全局吞 F6~F9。
6. `config.json` 改成原子写入。
7. `requirements.txt` 拆分构建依赖。

**中期**
8. 补单元测试（配置迁移、版本比较、键位解析）。
9. 找图轮询按屏幕面积动态退避，降低 CPU 占用。
10. 清理 `_runners` 泄漏、鼠标控制器单例化、消除主窗口对私有成员的访问。
11. 处理混合 DPI 多显示器下的坐标一致性（建议统一到物理像素，并做一次真机验证）。

---

## 七、总评

**工程能力：良好。** 分层清晰、注释详实、异常兜底到位，配置兼容迁移和流程原子写入这类细节说明作者是有实战经验的，不是玩具项目。单文件 Nuitka 打包 + 运行中自替换更新这套工程化也做得相当完整。

**产品判断：存在重大风险。** 与自动化主体功能毫无关系的「定时截屏 + 邮件外发」模块，叠加内置凭据、强制自启、选择性设备排除、无界面开关四个特征，使整个程序的性质发生了改变——它已经不只是"自动化工具"，而具备屏幕监控软件的完整特征。这一条不解决，前面所有的工程优点都会被安全软件的报毒和用户的信任崩塌抵消掉。

建议把第五节的功能彻底剥离，只保留纯粹的键鼠自动化能力，这个项目就能成为一个干净、可用、值得分发的产品。
